import SwiftUI

enum Theme {
    static let bg = Color(red: 0.06, green: 0.07, blue: 0.10)
    static let panel = Color(red: 0.11, green: 0.12, blue: 0.17)
    static let card = Color.white.opacity(0.06)
    static let line = Color.white.opacity(0.12)
    static let text = Color(red: 0.96, green: 0.96, blue: 0.98)
    static let sub = Color(red: 0.66, green: 0.68, blue: 0.74)
    static let accent = Color(red: 0.36, green: 0.82, blue: 0.62)   // ミントグリーン
    static let coin = Color(red: 1.0, green: 0.80, blue: 0.28)      // 硬貨リング
    static let item = Color(red: 0.36, green: 0.72, blue: 1.0)      // 商品ボックス
}
